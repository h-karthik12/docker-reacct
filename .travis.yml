sudo: required
services:
 - docker

before_install:
 - docker build -t karthik12gowda/docker-react -f Dockerfile.dev .

script:
 - docker run -e CI=true karthik12gowda/docker-react npm run test -- --coverage
  
